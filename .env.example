# Environment Variables Example
# Copy this file to .env and fill in your values for local development

# JWT Secret for Cloudflare Workers Authentication
# Generate with: openssl rand -base64 32
# In production, use: echo "secret" | npx wrangler secret put JWT_SECRET
JWT_SECRET=your-local-dev-secret-change-me

# Supabase Configuration (optional - used for patient management)
# Create a project at https://supabase.com and get these values from project settings
VITE_SUPABASE_URL=your-supabase-project-url
VITE_SUPABASE_ANON_KEY=your-supabase-anon-key
